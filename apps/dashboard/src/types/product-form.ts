/**
 * Dashboard namespaces that expose product form keys (`form.*`, `sections.*`, etc.).
 * Extend with e.g. `"products.edit"` when the edit flow ships matching messages.
 */
export type ProductFormTranslationNamespace = "products.create";
